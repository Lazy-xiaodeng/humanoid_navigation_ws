# 修改日志

## 2026-05-26 15:30:37 CST

- 审查 SC recovery 相关节点的日志可观测性和逻辑旁路，确认新功能会在 `debug_output.txt` 中输出成功接受、软验收拒绝、软验收放行、NDT rejected 后重试等关键原因。
- 修复 `src/humanoid_navigation2/humanoid_navigation2/localization_odom_fusion.py`: 非 JSON legacy recovery 成功消息不再绕过 `_validate_lost_recovery_soft()`，和 JSON `localization_recovered` 走同一条软验收路径。
- 增强 LOST recovery 成功日志: `LOST→HEALTHY` 时输出恢复后的 `/pcl_pose`、`ndt_state`、`converged`、`error`，便于从日志判断 fusion 接受了哪个 NDT 结果。
- 增强软验收跳过原因日志: 软验收关闭、NDT 状态缺失、NDT 状态过旧、`/pcl_pose` 缺失/过旧、odom 信息不足、odom 位移过大都会明确打印原因。
- 修正 `src/humanoid_navigation2/launch/navigation2_fusion_sc.launch.py` 和 `localization_odom_fusion.py` 中容易误导排查的注释: HEALTHY 时 NDT 发布 `map->odom`，DEGRADED/TRANSITIONING 时 fusion 接管，LOST 时等待 SC/HDL recovery bridge 通过 `/initialpose` 让 NDT 重建定位。
- 已执行 `python3 -m py_compile src/humanoid_navigation2/humanoid_navigation2/scancontext_to_initialpose.py src/humanoid_navigation2/humanoid_navigation2/localization_odom_fusion.py src/humanoid_navigation2/launch/navigation2_fusion_sc.launch.py`，语法检查通过。

## 2026-05-26 15:16:53 CST

- 修复 `src/humanoid_navigation2/humanoid_navigation2/localization_odom_fusion.py` 中 LOST recovery 成功事件的无条件接受问题: 收到 `localization_recovered` 后先执行 `_validate_lost_recovery_soft()`，通过后才 `LOST→HEALTHY`。
- 新增 LOST recovery 软验收参数: `recovery_pose_soft_gate_enabled`、`recovery_pose_max_xy_error_m`、`recovery_pose_accept_if_ndt_error_below`、`recovery_pose_skip_odom_after_displacement_m`、`recovery_pose_max_status_age_sec`、`recovery_pose_max_pcl_age_sec`。
- 新增 `_validate_lost_recovery_soft()`：只拦截明显离谱的恢复结果；不使用 yaw 硬拒绝；odom/frozen 信息缺失、odom 位移较大或 NDT error 极低时放行，避免正确 SC/NDT recovery 被 odom 累计误差误杀。
- 更新 `src/humanoid_navigation2/launch/navigation2_fusion_sc.launch.py`，显式配置 LOST recovery 软验收参数，方便实机调参。
- 文档追加 `NDT_FUSION_SC_MODIFICATION_REPORT.md` 第十三节，记录本次修改方案、代码出处、判断逻辑和修改后的作用效果。
- 已执行 `python3 -m py_compile src/humanoid_navigation2/humanoid_navigation2/localization_odom_fusion.py src/humanoid_navigation2/launch/navigation2_fusion_sc.launch.py`，语法检查通过。

## 2026-05-26 14:31:50 CST

- 分析 `/home/ubuntu/humanoid_ws/debug_output.txt` 中 2026-05-26 13:50:55 CST 启动的 SC 实机日志: 首次 SC accepted 在 13:51:51，fusion 在 13:51:52 进入 `INITIALIZING→HEALTHY`，NDT 在 13:51:59 验收 initialpose；之后 13:52:09 和 13:52:48 仍出现无必要的 SC accepted 和 `/initialpose` 重复发布。
- 修复 `src/humanoid_navigation2/humanoid_navigation2/scancontext_to_initialpose.py`: 有 pending `/initialpose` 或正在等待 NDT 验收时不再继续触发 SC；非 recovery 状态下忽略 SC `best_pose`；新增 `complete_recovery()` 统一清理 `recovery_active`、`recovery_waiting_for_ndt`、pending initialpose、external recovery 窗口和 NDT 稳定计数。
- 文档追加 `NDT_FUSION_SC_MODIFICATION_REPORT.md` 第十二节，记录本次日志时间线、风险分析、修改代码出处、修改后的预期表现和验证方法。
- 已执行 `python3 -m py_compile src/humanoid_navigation2/humanoid_navigation2/scancontext_to_initialpose.py`，语法检查通过。

## 2026-05-11 10:09:24 CST

- 修改前已创建 git checkpoint：`31c3045 Checkpoint before TF chain fix`。
- 将长期协作规则写入本地记忆：以后涉及非平凡代码修改前先确认或创建 git checkpoint；每次代码、配置、文档修改都要按时间写入本修改日志。
- 修复定位 TF 发布逻辑：`HdlLocalizationNodelet::publish_odometry()` 不再在缺少 `odom -> base_footprint` 时回退发布 `map -> base_footprint`，避免破坏标准 TF 链 `map -> odom -> camera_init -> body -> base_footprint`。
- 定位节点发布 `map -> odom` TF 和 `/hdl/odom` 时使用当前 ROS 时间，和 Fast-LIO 当前 `camera_init -> body` 的时间戳策略保持一致，降低 TF 缓存时间不匹配导致的 RViz 和 costmap transform 错误。
- 当定位节点无法查询 `odom -> base_footprint` 时，本帧跳过 `map -> odom` TF 发布，并通过节流日志输出原因，方便后续诊断。
