# 一键启动脚本使用说明

最后更新：2026-05-22

本文说明 `/home/ubuntu/humanoid_ws/start_navigation.sh` 的使用方法、脚本功能、运行顺序、日志位置，以及残留进程清理逻辑。

## 1. 脚本入口

当前一键启动脚本只有一个正式入口：

```bash
cd /home/ubuntu/humanoid_ws
./start_navigation.sh
```

脚本最终只会主动启动这一条 ROS 2 launch 命令：

```bash
ros2 launch humanoid_bringup robot_real.launch.py use_sim_time:=false
```

也就是说，系统入口仍然是 `robot_real.launch.py`。脚本里出现的 `robot_state_publisher`、`rslidar_sdk_node`、`fastlio_mapping`、`websocket_server_node` 等名称，不是脚本逐个启动的命令，而是用于检测和清理上一次启动后可能残留的子进程。

## 2. 脚本可以实现的功能

`start_navigation.sh` 当前主要实现以下功能：

1. 自动进入工作空间 `/home/ubuntu/humanoid_ws`。
2. 自动创建本次启动专用 debug 日志文件。
3. 自动指定本次启动的 ROS 日志目录。
4. 自动加载 ROS 2 Jazzy 环境。
5. 自动编译工作空间。
6. 编译完成后自动刷新工作空间环境变量。
7. 检查关键 Python 包是否已经正确安装到当前环境。
8. 检测上一次一键启动留下的导航主进程和相关子进程。
9. 如果发现旧进程残留，先清理旧进程，再启动新的导航系统。
10. 后台启动 `robot_real.launch.py`。
11. 记录本次启动的主进程 PID。
12. 启动后等待 2 秒，检查主 launch 是否立刻崩溃。
13. 如果启动成功，脚本返回成功；导航系统继续在后台运行。

## 3. 日志位置

脚本不再反复覆盖同一个 `debug_output.txt`，而是每次启动都新建一份日志。

日志根目录：

```bash
/home/ubuntu/humanoid_ws/debug_logs/
```

脚本自身和屏幕输出日志：

```bash
/home/ubuntu/humanoid_ws/debug_logs/start_navigation_YYYYMMDD_HHMMSS.log
```

例如：

```bash
/home/ubuntu/humanoid_ws/debug_logs/start_navigation_20260522_153012.log
```

ROS 2 自己生成的日志目录：

```bash
/home/ubuntu/humanoid_ws/debug_logs/ros_YYYYMMDD_HHMMSS/
```

例如：

```bash
/home/ubuntu/humanoid_ws/debug_logs/ros_20260522_153012/
```

查看最近一次脚本日志：

```bash
ls -lt /home/ubuntu/humanoid_ws/debug_logs/start_navigation_*.log | head
```

实时查看某次启动日志：

```bash
tail -f /home/ubuntu/humanoid_ws/debug_logs/start_navigation_20260522_153012.log
```

## 4. 运行后的完整流程

运行：

```bash
./start_navigation.sh
```

脚本内部流程如下。

### 4.1 初始化日志

脚本先生成启动时间：

```bash
START_TIME="$(date +%Y%m%d_%H%M%S)"
```

然后根据启动时间生成两个路径：

```bash
LOG_FILE="$WORKSPACE/debug_logs/start_navigation_${START_TIME}.log"
ROS_LOG_DIR="$WORKSPACE/debug_logs/ros_${START_TIME}"
```

之后脚本会把终端输出和错误输出同时写入本次日志：

```bash
exec > >(tee -a "$LOG_FILE") 2>&1
```

所以运行脚本时看到的内容，基本也会保存在本次 `start_navigation_时间.log` 中。

### 4.2 检查 ROS 基础环境

脚本检查 ROS 2 Jazzy 环境文件是否存在：

```bash
/opt/ros/jazzy/setup.bash
```

如果这个文件不存在，说明 ROS 2 基础环境异常，脚本会直接退出。

### 4.3 加载 ROS 2 环境

脚本执行：

```bash
source /opt/ros/jazzy/setup.bash
```

这一步让当前 shell 能找到 `ros2`、`colcon`、基础 ROS 包等命令和环境。

### 4.4 编译工作空间

脚本执行：

```bash
colcon build --symlink-install --cmake-args -DCMAKE_BUILD_TYPE=Release
```

这一步会编译 `/home/ubuntu/humanoid_ws/src/` 下的工作空间包。

使用 `--symlink-install` 的好处是，Python 脚本、launch 文件、配置文件修改后通常更容易快速生效。

使用 `-DCMAKE_BUILD_TYPE=Release` 是为了让 C++ 节点按 Release 模式编译，运行性能更好。

### 4.5 检查 install 环境

编译结束后，脚本检查：

```bash
/home/ubuntu/humanoid_ws/install/setup.bash
```

如果编译后仍然没有这个文件，说明工作空间没有正确生成安装空间，脚本会退出。

### 4.6 刷新工作空间环境变量

脚本执行：

```bash
source /home/ubuntu/humanoid_ws/install/setup.bash
```

这一步非常关键。它保证后面启动 `robot_real.launch.py` 时，使用的是刚刚编译后的最新包、最新 launch 文件和最新 Python 节点。

### 4.7 检查关键 Python 包

脚本会检查这两个 Python 包元数据：

```text
humanoid-navigation
humanoid-websocket
```

如果检查失败，通常表示工作空间编译或安装环境没有正确生效。脚本会退出，避免后面启动时才出现更难定位的问题。

### 4.8 清理旧进程

刷新环境变量后，脚本会执行旧进程清理：

```bash
cleanup_old_navigation_processes
```

清理分两部分。

第一部分是读取上次启动记录：

```bash
/home/ubuntu/humanoid_ws/.start_navigation.pid
```

这个文件保存的是上一次 `robot_real.launch.py` 的主 launch 进程 PID。

第二部分是用 `pgrep -f` 检查一些导航相关进程名。这里的 `local patterns=(...)` 是“旧进程清理名单”，不是“启动名单”。

举例：

```bash
"ros2 launch humanoid_bringup robot_real.launch.py"
"ros2 launch humanoid_navigation2 navigation_stack.launch.py"
"rslidar_sdk_node"
"fastlio_mapping"
"websocket_server_node"
```

这些名称存在的原因是：`robot_real.launch.py` 会间接启动很多子 launch 和节点。如果上一次启动异常退出，有时主 launch 已经没了，但子节点仍然残留。下次直接启动新的系统时，就可能出现节点重名、端口占用、雷达重复发布、TF 重复发布等问题。

旧进程清理顺序：

1. 先发送 `SIGINT`，相当于正常 Ctrl+C。
2. 最多等待 10 秒。
3. 如果还有进程没退出，再发送 `SIGTERM`。
4. 最多等待 5 秒。
5. 如果仍然没退出，最后发送 `SIGKILL`。

这种顺序的目的，是优先让 ROS 2 launch 和节点正常收尾，只有在进程卡死时才强制清理。

### 4.9 启动新导航系统

旧进程清理完成后，脚本启动新的正式系统：

```bash
ros2 launch humanoid_bringup robot_real.launch.py use_sim_time:=false &
```

末尾的 `&` 表示后台启动。

因此脚本本身不需要一直占着终端。脚本确认启动成功后会退出，但 ROS 2 导航系统会继续在后台运行。

### 4.10 记录本次主进程 PID

启动后，脚本记录主 launch 的 PID：

```bash
/home/ubuntu/humanoid_ws/.start_navigation.pid
```

下次再次运行一键启动脚本时，会优先根据这个 PID 清理上一次启动的主进程。

### 4.11 启动健康检查

脚本等待 2 秒：

```bash
sleep 2
```

然后检查主 launch 进程是否仍然存在。

如果还活着，会输出类似：

```text
NAVIGATION_STARTED_SUCCESSFULLY_PID_12345
Debug log: /home/ubuntu/humanoid_ws/debug_logs/start_navigation_20260522_153012.log
```

这表示一键启动脚本认为系统已经成功拉起。

如果 2 秒内主 launch 退出了，会输出：

```text
ERROR: Navigation process crashed immediately.
```

这表示启动阶段发生了明显错误，需要查看本次日志文件。

## 5. robot_real.launch.py 实际会启动什么

一键脚本只启动 `robot_real.launch.py`，但 `robot_real.launch.py` 内部会继续包含多个子 launch。

整体结构如下：

```text
start_navigation.sh
  -> ros2 launch humanoid_bringup robot_real.launch.py use_sim_time:=false
       -> humanoid_description/display.launch.py
       -> humanoid_navigation2/navigation_stack.launch.py
       -> humanoid_navigation/navigation.launch.py
       -> humanoid_websocket/websocket_server.launch.py
       -> humanoid_locomotion/locomotion.launch.py
       -> rviz2
```

其中：

```text
display.launch.py
  -> robot_state_publisher
  -> joint_state_publisher
```

`robot_state_publisher` 负责发布机器人模型相关 TF。

`joint_state_publisher` 负责发布关节状态，配合机器人模型使用。

```text
navigation_stack.launch.py
  -> rslidar_sdk_node
  -> fast_lio_node
  -> point_cloud_filter_node
  -> map_server
  -> lidar_localization
  -> hdl_global_localization_node
  -> hdl_bootstrap_container
  -> hdl_bootstrap_to_initialpose
  -> robot_realpose_publisher
  -> periodic_clearing_publisher
  -> periodic_clearing_3d_publisher
  -> wait_for_localization_tf
  -> planner_server
  -> controller_server
  -> behavior_server
  -> bt_navigator
```

`rslidar_sdk_node` 是 Robosense 雷达驱动节点，负责接收和发布雷达点云。

`fast_lio_node` 是 Fast-LIO 建图/里程计相关节点，负责融合雷达和 IMU，发布里程计和点云。

`point_cloud_filter_node` 是点云滤波节点，负责把原始点云处理成导航可用的点云。

`map_server` 负责加载 2D 栅格地图。

`lidar_localization` 负责激光定位，发布地图到里程计之间的定位关系。

`planner_server`、`controller_server`、`behavior_server`、`bt_navigator` 是 Nav2 的核心导航节点。

```text
navigation.launch.py
  -> dynamic_waypoints_manager
  -> navigation_state_manager
```

`dynamic_waypoints_manager` 负责 APP 点位增删改查和动态点位文件。

`navigation_state_manager` 负责监听导航状态、管理导航任务和恢复流程。

```text
websocket_server.launch.py
  -> websocket_server_node
  -> data_integration_node_recoverable
  -> websocket_client_node
  -> message_bridge_node
```

这些节点负责 APP 通信、机器人本体 WebSocket 通信、数据整合和消息桥接。

```text
locomotion.launch.py
  -> facial_driver
```

`facial_driver` 负责表情或动作相关控制。

## 6. 为什么清理名单里有很多 patterns

`local patterns=(...)` 里的内容是为了处理“上一次没关干净”的情况。

例如，正常情况下你只运行：

```bash
./start_navigation.sh
```

它只会启动：

```bash
robot_real.launch.py
```

但是 `robot_real.launch.py` 会继续启动雷达、定位、Nav2、WebSocket、动作等很多节点。如果电脑断电、终端异常关闭、launch 崩溃、手动 kill 不完整，就可能留下部分子节点。

下次启动前，如果不清理这些残留，可能出现：

1. 雷达节点重复启动。
2. WebSocket 端口被旧进程占用。
3. ROS 节点重名。
4. TF 重复发布。
5. Nav2 lifecycle 节点状态异常。
6. 日志里出现 `Publisher already registered for node name`。
7. APP 连接到旧服务，而不是新服务。

所以脚本会把这些可能残留的子进程也纳入清理范围。

## 7. 常用操作

### 7.1 正常启动

```bash
cd /home/ubuntu/humanoid_ws
./start_navigation.sh
```

### 7.2 查看本次启动日志

脚本启动时会打印：

```text
Debug log: /home/ubuntu/humanoid_ws/debug_logs/start_navigation_YYYYMMDD_HHMMSS.log
```

直接查看这份文件即可。

### 7.3 查看最近日志

```bash
ls -lt /home/ubuntu/humanoid_ws/debug_logs/start_navigation_*.log | head
```

### 7.4 实时跟踪日志

```bash
tail -f /home/ubuntu/humanoid_ws/debug_logs/start_navigation_YYYYMMDD_HHMMSS.log
```

### 7.5 再次启动

如果系统已经启动过，再次运行：

```bash
./start_navigation.sh
```

脚本会先编译和刷新环境，然后检测旧进程。如果发现旧进程，会先清理旧进程，再启动新的一套系统。

### 7.6 判断脚本是否启动成功

看到类似输出：

```text
NAVIGATION_STARTED_SUCCESSFULLY_PID_12345
```

说明主 launch 进程已经成功启动，并且没有在前 2 秒内崩溃。

注意：这只代表启动阶段没有立刻崩溃，不代表所有传感器、定位、Nav2 生命周期都已经完全就绪。完整状态仍需要结合日志、`ros2 node list`、`ros2 topic list`、APP 连接状态和 RViz 观察。

## 8. 重要文件说明

一键启动脚本：

```bash
/home/ubuntu/humanoid_ws/start_navigation.sh
```

本次脚本日志：

```bash
/home/ubuntu/humanoid_ws/debug_logs/start_navigation_YYYYMMDD_HHMMSS.log
```

本次 ROS 日志目录：

```bash
/home/ubuntu/humanoid_ws/debug_logs/ros_YYYYMMDD_HHMMSS/
```

上次主 launch PID：

```bash
/home/ubuntu/humanoid_ws/.start_navigation.pid
```

正式系统入口 launch：

```bash
/home/ubuntu/humanoid_ws/src/humanoid_bringup/launch/robot_real.launch.py
```

导航栈主 launch：

```bash
/home/ubuntu/humanoid_ws/src/humanoid_navigation2/launch/navigation_stack.launch.py
```

应用层导航 launch：

```bash
/home/ubuntu/humanoid_ws/src/humanoid_navigation/launch/navigation.launch.py
```

WebSocket launch：

```bash
/home/ubuntu/humanoid_ws/src/humanoid_websocket/launch/websocket_server.launch.py
```

## 9. 故障排查建议

### 9.1 脚本启动失败

优先查看本次脚本日志：

```bash
tail -n 200 /home/ubuntu/humanoid_ws/debug_logs/start_navigation_YYYYMMDD_HHMMSS.log
```

重点看：

1. `colcon build` 是否失败。
2. `install/setup.bash` 是否存在。
3. Python 包元数据检查是否失败。
4. 旧进程是否清理失败。
5. `robot_real.launch.py` 是否启动后立刻退出。

### 9.2 发现旧进程被清理

日志里如果出现：

```text
Detected previous navigation processes:
```

说明脚本发现了上一次残留进程。

后面会打印：

```text
Process details before cleanup:
```

可以看到被清理进程的 PID 和命令行。

### 9.3 启动成功但系统不工作

如果脚本打印了：

```text
NAVIGATION_STARTED_SUCCESSFULLY_PID_xxx
```

但 APP、定位或导航仍然异常，需要继续看本次日志中后续的 ROS 输出，重点检查：

1. 雷达是否有数据。
2. Fast-LIO 是否启动。
3. 地图是否加载成功。
4. 定位是否成功发布 TF。
5. Nav2 lifecycle 是否激活。
6. WebSocket 端口是否正常监听。

## 10. 一句话总结

`start_navigation.sh` 是现场使用的一键启动入口。它负责“记录独立日志、编译、刷新环境、清理旧残留、启动 `robot_real.launch.py`、记录新 PID、做启动初检”。真正的机器人系统仍然由 `robot_real.launch.py` 统一拉起，脚本里的进程名列表只是为了下次启动前清理旧残留，不是额外的启动步骤。
